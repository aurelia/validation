'use strict';

System.register([], function (_export, _context) {
  "use strict";

  var validateTrigger;
  return {
    setters: [],
    execute: function () {
      _export('validateTrigger', validateTrigger = {
        blur: 'blur',

        change: 'change',

        manual: 'manual'
      });

      _export('validateTrigger', validateTrigger);
    }
  };
});